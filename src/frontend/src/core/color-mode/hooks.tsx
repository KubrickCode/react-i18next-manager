import {
  useColorMode as useChakraColorMode,
  useColorModeValue as useChakraColorModeValue,
} from "@chakra-ui/react";

export const useColorMode = useChakraColorMode;
export const useColorModeValue = useChakraColorModeValue;
